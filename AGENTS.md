# AGENTS.md

## Overview

A single `.docx` document — no source code, no package manifest, no build tooling.

## Working in this repo

- The only file is `Тревел блоги.docx` (Travel Blogs).
- The repo has no git history. Initialize with `git init` before committing.
- No tests, linters, or formatters configured.
